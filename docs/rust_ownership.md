# Rust Ownership

Rust uses ownership, borrowing and lifetimes to guarantee memory safety without a garbage collector. Each value has one owner. References let code borrow data without moving ownership.

This document is useful for testing search queries such as Rust, ownership, borrowing, lifetime and memory safety.
the 
