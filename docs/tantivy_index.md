# Tantivy Index

Tantivy is a high-performance full-text search engine library written in Rust. It supports inverted indexes, ranking scores, query parsing and fast retrieval.

The system builds an index from local Markdown, TXT, source code and JSON documents. Search results include score, title, path and snippet.
